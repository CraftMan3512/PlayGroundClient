using System;
using System.Collections.Generic;
using System.Text;

namespace PlayGroundServer
{
    class Constants
    {
        public const int TICKS_PER_SEC = 30;
        public const int MS_PER_TICK = 1000 / TICKS_PER_SEC;
        public const int AUTOSAVE_INTERVAL_MS = 30000;
        public const int TITLESCREEN_SCENEINDEX = 1;
        public const int HUBWORD_SCENEINDEX = 2;

    }
}